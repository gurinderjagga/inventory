{
  "name": "inventory-management",
  "version": "1.0.0",
  "description": "Company-wise inventory management and invoicing system",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node database/seed.js",
    "test": "node --test --test-concurrency=1 --require ./tests/helpers/env.js \"tests/**/*.test.js\"",
    "test:watch": "node --test --watch --test-concurrency=1 --require ./tests/helpers/env.js \"tests/**/*.test.js\""
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "compression": "^1.8.1",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.6",
    "decimal.js": "^10.6.0",
    "dotenv": "^17.4.2",
    "etag": "^1.8.1",
    "express": "^4.18.2",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0",
    "jsonwebtoken": "^9.0.2",
    "pdfkit": "^0.15.0",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
